# Obsidian — личные настройки рабочего пространства (не нужны коллегам)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json

# Корзина Obsidian
.trash/

# LaTeX — временные файлы компиляции (не нужны в репозитории)
*.aux
*.log
*.bcf
*.blg
*.bbl
*.out
*.run.xml
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
texput.log

# LaTeX — скомпилированные PDF (тяжёлые бинарники, регенерируются)
sources/chapters/**/*.pdf
sources/*.pdf

# Учебники и источники в PDF (авторские права + большой размер)
# Сводки-index.md хранятся в git, сами PDF — только локально
sources/маркеев/*.pdf
sources/сивухин-т1/*.pdf
sources/kleppner/*.pdf
sources/кикоин-9кл/*.pdf
sources/балашов-мякишев/*.pdf
sources/берклеевский-т1/*.pdf
sources/кингсеп-т1/*.pdf
sources/морин/*.pdf
sources/аносов/*.pdf
sources/пятницкий/*.pdf
sources/kalda/*.pdf
sources/бутиков-кеплер/*.pdf

# Системные файлы Windows
Thumbs.db
desktop.ini

# macOS (на случай если коллеги на маке)
.DS_Store
