---
status: draft
tags: [кинематика, относительное-движение, метод-СО, минимальное-расстояние, относительная-скорость]
level: 2
sources:
  - "adapted_relative_mov.tex"
latex_packages: [physics, unicode-math]
relations:
  requires:
    - сложение-скоростей
  leads_to:
    - отражение-от-движущейся-поверхности
---

# Метод подвижной системы отсчёта

Заметка описывает один из главных приёмов кинематики: переход в систему отсчёта одного из тел. Это превращает задачу о двух движущихся объектах в задачу об одном движущемся и одном покоящемся, что часто существенно упрощает геометрию. Строится на [[сложение-скоростей]].

## ★ Суть метода

Многие задачи на относительное движение сводятся к нахождению скорости одного тела относительно другого. Перейдём в систему отсчёта тела $A$: в ней $A$ покоится, а тело $B$ движется со скоростью
$$\vec{v}_{BA} = \vec{v}_B - \vec{v}_A$$
по прямой линии (если оба движутся равномерно). Задача о двух движущихся телах стала задачей об одном.

> [!definition]
> **Скорость тела $B$ относительно тела $A$** — скорость $B$ в системе отсчёта $A$:
> $$\vec{v}_{BA} = \vec{v}_B - \vec{v}_A$$
> где $\vec{v}_A$ и $\vec{v}_B$ — скорости тел в исходной (лабораторной) системе.

## ★ Минимальное расстояние

Пусть два тела $A$ и $B$ движутся прямолинейно и равномерно. В системе отсчёта $A$ тело $B$ движется с постоянной скоростью $\vec{v}_{BA}$ по прямой линии — точечный объект $B$ прочерчивает прямую траекторию мимо неподвижного $A$.

Минимальное расстояние между $A$ и $B$ — это расстояние от неподвижного $A$ до прямой траектории $B$ в системе $A$, то есть длина перпендикуляра, опущенного из $A$ на эту прямую. Это значительно проще, чем минимизировать $|\vec{r}_B(t) - \vec{r}_A(t)|$ напрямую.

Разбор с конкретными числами — [[пример-два-корабля]].

## ★ Задачи о переправе

Классический тип: пловец (или лодка) пересекает реку с течением. В системе воды вектор скорости пловца имеет фиксированный модуль $v$ и произвольное направление. Скорость в системе Земли — сумма скорости течения $\vec{u}$ и выбранного направления:
$$\vec{v}_{\text{абс}} = \vec{v}_{\text{отн}} + \vec{u}$$

Задача о минимальном времени: поперечная компонента абсолютной скорости пловца максимальна, когда он плывёт строго поперёк (перпендикулярно берегу). При этом течение сносит пловца, но время переправы минимально: $t_{\min} = L/v$.

Задача о кратчайшем пути: абсолютная скорость должна быть направлена поперёк реки. Для этого относительная скорость $\vec{v}_{\text{отн}}$ выбирается под углом против течения так, чтобы скомпенсировать $\vec{u}$. Из треугольника скоростей: $\sin\varphi = u/v$, поперечная компонента $v_\perp = \sqrt{v^2 - u^2}$, время $t = L/\sqrt{v^2 - u^2}$.

Полный разбор обоих вопросов с треугольниками скоростей — [[пример-пловец-через-реку]].

## Связи и продолжения

Метод подвижной системы отсчёта использует закон сложения скоростей ([[сложение-скоростей]]). Тот же приём применяется при отражении от движущейся поверхности ([[отражение-от-движущейся-поверхности]]).

- ← [[сложение-скоростей]] — закон, который лежит в основе метода
- → [[отражение-от-движущейся-поверхности]] — переход в СО стенки сводит задачу к отражению от неподвижного
- → [[пример-два-корабля]] — минимальное расстояние: перпендикуляр из $A$ к траектории $B$
- → [[пример-пловец-через-реку]] — два варианта задачи о переправе: минимальное время и кратчайший путь
