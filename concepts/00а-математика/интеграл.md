---
status: draft
tags: [математика, интеграл, анализ]
level: ⭐
latex_packages: [physics, unicode-math]
---

# Интеграл ⭐

## Определение

**Определённый интеграл** $\int_a^b f(x)\,dx$ — площадь фигуры, ограниченной графиком $f(x)$, осью $x$ и вертикалями $x=a$, $x=b$ (с учётом знака: площадь ниже оси отрицательна).

**Неопределённый интеграл** $\int f(x)\,dx = F(x) + C$, где $F'(x) = f(x)$ — первообразная.

**Теорема Ньютона–Лейбница:**
$$\int_a^b f(x)\,dx = F(b) - F(a)$$

## Физический смысл в кинематике

Площадь под графиком $v_x(t)$ на $[t_1, t_2]$ равна изменению координаты:
$$x(t_2) - x(t_1) = \int_{t_1}^{t_2} v_x(t)\,dt$$

Площадь под $a_x(t)$ — изменению скорости:
$$v_x(t_2) - v_x(t_1) = \int_{t_1}^{t_2} a_x(t)\,dt$$

## Таблица интегралов

| $f(x)$ | $\int f(x)\,dx$ |
|--------|----------------|
| $x^n\ (n\neq-1)$ | $\dfrac{x^{n+1}}{n+1} + C$ |
| $\frac{1}{x}$ | $\ln|x| + C$ |
| $\sin x$ | $-\cos x + C$ |
| $\cos x$ | $\sin x + C$ |
| $e^x$ | $e^x + C$ |

## Интегрирование вектора

Интеграл вектора берётся покомпонентно:
$$\int \vec{v}(t)\,dt = \hat{i}\int v_x\,dt + \hat{j}\int v_y\,dt + \hat{z}\int v_z\,dt$$

Это используется при нахождении $\vec{r}(t)$ из $\vec{v}(t)$, и $\vec{v}(t)$ из $\vec{a}(t)$.

## Связанные заметки

- [[производная]] ⭐ — обратная операция
- [[предел]] — основа определения
- [[concepts/01-одномерная/мгновенная-скорость|Мгновенная скорость]]
- [[concepts/00б-оду/index|ОДУ]] — интегрирование уравнений движения

## LaTeX-код

```tex
Изменение координаты через интеграл скорости:
\[
x(t_2) - x(t_1) = \int_{t_1}^{t_2} v_x(t)\,\dd{t}.
\]
```
